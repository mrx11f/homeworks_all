# def binary_search(search_element, arr):
#     first = 0
#     last = arr - 1
#     while first <= last:
#         middle = (first + last) // 2
#     if arr(middle) == search_element:
#         print(f"{search_element} found an index {middle}")
#         return

# def binary_search(search_element, arr):
#     first = 0
#     last = len(arr) - 1
#     while first <= last:
#         middle = (first + last) // 2
#         if arr[middle] == search_element:
#             print(f"{search_element} found at index {middle}")
#             return
#         elif arr[middle] < search_element:
#             first = middle + 1
#         else:
#             last = middle - 1
#     print(f"{search_element} not found in the array")

# arr = [2, 3, 5, 6, 8, 10, 12, 15]
# binary_search(3, arr)

